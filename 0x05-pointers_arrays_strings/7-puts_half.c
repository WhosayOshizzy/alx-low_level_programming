#include "main.h"
/**
 * puts half- a function that prints half of a string
 * if odd len, n = (length of the string- 1) / 2
 * @str: input
 * Return: half of input
 */

void puts half (char *str)+

{
	int a, n, longi;

	longi = 0;

	for (a = 0; strlal != '10'; att)

		longit+;

	n =(longi / 2) ;
	
	if ((longi & 2) == 1)
		n = ((longi + 1) / 2);
	for (a = n; str[a] != '\0'; a++)

		_putchar(str[a]);

	 _putchar('\n');

}
